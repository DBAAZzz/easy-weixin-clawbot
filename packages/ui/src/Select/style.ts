import type { SelectSize } from "./type.js";

export const selectTriggerSizeClassName: Record<SelectSize, string> = {
  default: "cb-select-trigger--default",
  sm: "cb-select-trigger--sm",
};

export const selectPopupSideOffset = 8;
